# AGENTS.md — Skills 仓库

## 一、Skill 设计核心理念

### 1.1 Skill 的本质：Prompt 优先，代码为辅

**Skill 不是程序，而是给模型的专业方法论。** 真正完成任务的是模型本身，`SKILL.md` 才是核心产物。脚本只承担模型无法直接完成的原子能力：调用外部 API、读取本地文件/数据库、执行确定性计算或格式转换。判断、分析、措辞、结构组织、风险提示都交给模型。

一句话：**把"手"留给脚本，把"脑"留给模型。**

### 1.2 内容分层与渐进式加载

Skill 系统采用三层加载，token 经济性差异巨大：

| 层级 | 内容 | 加载时机 | 体量约束 |
|---|---|---|---|
| Metadata 层 | `name` + `description` | 始终在 context | ~100 词 |
| 主体层 | `SKILL.md` 正文 | skill 被触发时进入 | 理想 < 500 行 |
| 资源层 | `scripts/` `references/` `assets/` | 按需 | 无上限 |

`SKILL.md` 主体越精简，触发成本越低。大段领域知识、案例库、字段字典、表格速查应拆到 `references/`，主体只留纲要 + 指引（"当遇到 X 情况，读 `references/x.md`"）。

资源层三种角色不要混用：

- `scripts/` —— **可执行**代码。模型不读源码也能用，只看输入输出契约。一个脚本只做一件原子事。
- `references/` —— 模型**按需读**进上下文的文档。长方法论、行业知识、错误码字典、案例集放这里。
- `assets/` —— 模型**不读**，但出现在最终产物里的素材。模板文件、Logo、字体、样式表。

### 1.3 设计提示

以下信号通常说明 Skill 设计可以向"模型判断"方向优化：

- 脚本在拼接完整 Markdown 报告 —— 把"脑"交给了脚本；脚本应返回数据，报告组织交给模型。
- 脚本内部又调用另一个 LLM 做判断 —— 这类判断通常应交给主模型。
- `SKILL.md` 只有一句"运行 `xxx.py`" —— 缺少方法论。
- 大段 `if/else` 替模型做领域判断（如"PE > 30 就输出估值偏高"）。
- 一个脚本承担"取数 + 分析 + 生成结论"全链路。
- `description` 写成功能罗列而不是触发场景。
- 方法论塞爆 `SKILL.md` 主体而不拆 `references/` —— 拉高每次触发的 token 成本。

代码越大，Skill 越僵。保持脚本原子性，把领域知识下沉到 `references/`，Skill 才具备可组合性与可演化性。

---

## 二、SKILL.md 编写规范

`SKILL.md` 同时承担两个角色：**触发说明书**（让模型知道何时用）和 **工作手册**（让模型知道怎么用）。两者缺一不可。

### 2.1 标准结构

```markdown
---
name: skill-name
description: 触发的唯一字段，把"何时使用 + 做什么 + 不做什么"写在这里
---

# Skill Name

## 目标
## 适用场景与边界
## 领域方法论
## 工作流程
## 数据获取（脚本抓手）
## 输出规范
## 示例
```

需要完整模板时，参考 `references/SKILL.md.template`。

### 2.2 目标（Goal）

目标段同时说明三件事：

1. **做什么**：一句话概括产出形态。
2. **不做什么**：划清边界，避免过度触发。
3. **给谁用**：面向的使用者与典型场景。

示例："基于 Tushare 数据，为 A 股个股生成基本面 + 技术面 + 估值 + 股东结构的投研简报。不覆盖港股、美股、加密货币；不提供买卖建议；面向具备金融常识的主动投资者。"

### 2.3 description：触发的唯一通道

- `description` 是 skill 触发的唯一依据。"何时使用"的所有信息必须集中在 `description`，不能只在正文里写。
- 简单一步查询常常不触发 skill，即使描述完美匹配。Skill 主要在多步骤、专业判断、格式特定的任务上稳定触发。
- 模型默认有 undertrigger 倾向，所以 `description` 可以略带推力，明确写"当用户……时使用此 skill"。

**写作要求**：场景化而非关键词化；正例与反例同写；覆盖同一意图的多种表达方式；专名 + 任务动词都要有。

### 2.4 领域方法论（Philosophy）

领域方法论是 Skill 区别于"让模型凭通识回答"的关键。把领域内反复使用的思考框架显式写下来，例如：

- 股票分析：基本面看五力 + 财务质量，技术面看趋势 + 量价 + 结构，估值看历史分位 + 横纵比较。
- 代码审查：安全性 > 正确性 > 可读性 > 性能。
- 文档写作：金字塔原理、结论先行、一段一意。

如果方法论很长（超过 ~150 行），只在 `SKILL.md` 留纲要 + 索引，细节拆到 `references/`。

### 2.5 工作流程（Workflow）

按"可独立验证的产物"切分步骤，而不是按数量切分。每一步说明：做什么、用什么（脚本/reference/上下文）、产出什么。

### 2.6 能力抓手（脚本）

- 脚本清单 + 一句话职责。
- 调用方式、必需参数、返回格式。
- 依赖的环境变量（如 `TUSHARE_TOKEN`）。
- 常见错误与降级策略。

不要在此描述脚本内部实现，那是脚本自身 docstring 的职责。

### 2.7 输出规范

不写输出规范，模型产出会漂移。与其堆砌 MUST，不如解释为什么：

| 维度 | 怎么做 | 为什么 |
|---|---|---|
| 结构 | 标题层级、必选/可选段落明确列出 | 结构稳定，用户才能对比多份产出 |
| 长度 | 给字数范围或段落数上限 | 模型不约束就有越写越长的倾向 |
| 风格 | 中立分析性 / 报告性 / 口语化，选一种 | 风格不一致会让产出像不同人写的 |
| 数据呈现 | 小数位、单位、表格 vs bullet 统一 | 跨数据源拼装时格式会不一致 |
| 附加要求 | 免责声明、数据时间戳、引用来源 | 投研、医疗、法律类内容的可追溯性 |

#### 项目级文风默认

本仓库所有 skill 输出都要满足以下两条默认：

1. **文风讲人话，减少机械与僵硬。** 像跟懂行的人当面把一件事讲清楚那样写——句子通顺、有逻辑衔接，该解释因果、给判断时把话说透。避免模板腔、翻译腔、成段堆砌套话；避免为了凑结构把话说断、只丢关键词。
2. **同项罗列优先用 list，但每条要说人话。** 同一维度的多个条目拆成 bullet 或编号，一条一项；但每条用完整通顺的话写，不要退化成"字段A - 字段B - 字段C"式的横杠拼接。只有结构化对照（厂商 × 动作、指标 × 数值、节点 × 日期）才用表格。

### 2.8 示例

至少给一个完整的 input → output。示例的价值在于钉死边界条件——例如数据缺失、行业属性模糊、用户问得很短时，产出该长什么样。

---

## 三、安全审查

### 3.1 必守红线

- **禁止硬编码密钥**：代码中不得出现任何 key、token、password。
- **禁止提交 `.env` 文件**。
- **必须用环境变量读取敏感信息**：`TUSHARE_TOKEN`、`OPENAI_API_KEY` 等。
- **外部输入需校验**：URL、查询参数、用户提供的片段清洗后再使用。
- **Prompt 注入防护**：用户输入在拼接到 prompt 前需做边界标记或清洗。
- **最小权限**：脚本只请求完成任务所需的最少权限与数据。

### 3.2 skill-vetter

```bash
skill-vetter /path/to/skill
# 或在 skill 目录下
skill-vetter .
```

审查通过后方可发布。

---

## 四、环境变量

| 变量 | 描述 | 必需场景 |
|---|---|---|
| `TUSHARE_TOKEN` | Tushare Pro API token | 股票相关 Skill |
| `TAVILY_API_KEY` | Tavily 搜索 API key | 部分搜索类 Skill |
| `CLAWHUB_TOKEN` | ClawHub CLI token | 发布 Skill 必需 |

需要连接共享 PostgreSQL 的 Skill，参见 `references/postgresql.md`。

---

## 五、附录与参考索引

| 主题 | 文件 | 何时阅读 |
|---|---|---|
| ClawHub 发布流程与包边界 | `references/clawhub.md` | 需要发布 Skill 到 ClawHub 时 |
| 共享 PostgreSQL 连接 | `references/postgresql.md` | Skill 需要读写 PostgreSQL 时 |
| 跨 Agent Skill 同步 | `references/skill-sync.md` | 维护同步机制或排查同步问题时 |
