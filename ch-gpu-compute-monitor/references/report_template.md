# 报告结构模板

页面顺序按 PRD §5：顶部市场判断 + 供需拐点 → 市场成交价趋势 → 可用供给趋势
→ 市场报价 → 标准报价矩阵 → 数据源与采集状态。

渲染器靠**小节标题里的关键词**定位注入点，所以这几个标题的措辞不要随手改：
「市场判断」「市场成交价趋势」「数据源与采集状态」。

---

```markdown
---
date: 2026-08-25
window_days: 90
schema: gpu-compute-monitor/report/1.0
---

# GPU 算力价格与供给监控 · 2026-08-25

## 市场判断与供需拐点

一段话讲清整体结论：现在是偏紧、平衡还是偏松，靠什么看出来的，
以及有什么理由暂时不下定论。不要罗列指标，要给判断。

- **B200**：状态标签 + 一句话为什么。
- **H200 SXM**：同上。
- **H100 SXM**：同上。

评分未出时写"评分未出"和原因，别自己估一个。

## 市场成交价趋势

Ornn OCPI 日度结算，是唯一有 90 天完整历史的一层。先说三个型号各自的走势
和幅度，再说它们之间的关系（代际溢价在扩还是在收，能不能分辨是产能到位
还是需求迁移）。必要时用 A100 SXM4 / RTX 5090 这两个参照系判断"是整条
曲线在动还是某一代在动"。

| GPU | 最新（锚定日） | 1D | 7D | 30D | 90D 回撤 | 观测点 |
|---|---|---|---|---|---|---|

不可比的格子写「不可比（原因）」，不要留空也不要填近似值。

## 可用供给趋势

供给三件套分别怎么动的：offer 份额、可用 GPU 数、库存档位。
重点是回答"价格那边的动作，供给这边认不认"。

供给数据全部来自 Vast 与 Runpod，两家都没有历史接口，所以这一节的序列
从首次采集当天开始。上线初期只能给基线，不能给趋势——照实说，别硬凑。

## 市场报价

| 平台 | GPU | 档位 | Min | P25 | 中位 | P75 | 样本 |
|---|---|---|---|---|---|---|---|

样本不足门槛的行，分位数列写「样本不足」。

## 标准报价矩阵

| 平台 | GPU | On-demand | Spot/Preemptible | 承诺期 | 口径 | 核对日期 |
|---|---|---|---|---|---|---|

缺失值明确写「暂无」，人工核对的报价必须带核对日期，stale 的要标出来。

## 数据源与采集状态

一句话说今天数据完不完整，缺的那部分对结论有多大影响。
（逐源明细由渲染器从 evidence 注入，不用在 Markdown 里重抄一遍。）

## 今日变化与观察点

- 昨天说的哪条被证实/证伪了。
- 接下来几天盯什么，什么信号出现就该改判。
```

---

## Markdown 书写约束（渲染器的硬要求）

**每个段落写成一行，每条 bullet 写成一行，不要在句中硬折行。**
共享的 Markdown 引擎不支持 lazy continuation：bullet 的续行会掉出列表变成
独立段落，段落的续行会被当成断行渲染，读起来是一句话被拦腰截断。
写长句就让它长，交给浏览器折行。

同理，表格每行也必须是完整一行。

## 写作要求

**长度**：正文 900–1600 字。表格不计入。

**文风**：像跟懂行的人当面把事讲清楚——句子通顺、有逻辑衔接，给判断时把话说透。
不要模板腔，不要成段堆"综上所述""值得注意的是"。

**罗列**：同一维度的多个条目用 bullet，一条一项，但每条用完整通顺的话写完，
不要退化成"字段A - 字段B - 字段C"式的横杠拼接。结构化对照（平台 × GPU × 价格）才用表格。

**数字**：价格保留两位小数，单位统一 USD/GPU·hour；百分比保留一位小数并带正负号。
每个数字都要能在 evidence 里找到出处。

**边界**：
- 不给买卖建议、不给目标价、不给仓位。
- 不用前值冒充最新值。缺就写缺。
- `usable=false` 的变化率不许出现在结论里。
- 状态必须用文字表达，不能只靠颜色（PRD §10）。
- 说"可能是 X"时要说清凭什么这么想，以及什么证据会推翻它。
