# AI 日报 | YYYY-MM-DD

更新时间:HH:MM｜覆盖窗口:YYYY-MM-DD ~ MM-DD（承接上一期 YYYY-MM-DD）
数据来源:GitHub Trending / Product Hunt / Hacker News / RSS(OpenAI / Anthropic / Google AI / Meta AI / DeepMind / HuggingFace / Mistral / NVIDIA / The Decoder / Juya / AI Hot 等)/ 金十电报

**数据覆盖与缺口**（写成下面这张小表,**不要写成一大段文字**;抓取失败 / enrichment 缺失在"状态"列标注,不展开成段落）:

| 源 | 入库数 | 状态 |
|---|---:|---|
| Product Hunt | N | ✓ / enrichment 失败 X 个 |
| Hacker News | N | ✓ |
| GitHub Trending | N | ✓ |
| RSS | N | 部分 feed 失败(列名: OpenAI Blog / Microsoft AI 等 SSL/410/404) |
| 金十电报 | N | ✓(AI 相关占比低时注明) |

> **这份日报就是一张二维坐标系**:纵轴看模型与产品的能力 / 技术往哪走,横轴看 AI 产品实际被多少人、多少场景用上。两轴是主干,原始证据只在附录里枚举一次,不在正文反复出现。

> **📊 档位图例(全文统一,只用这两套词,别自造"实验到收敛中"这种中间档):**
> · 纵轴 · 能力矢量:`实验` → `收敛中` → `事实标准`(判档看证据强度不看热度;同一矢量当天 ≥2 个独立证据攻同一痛点,才算 `收敛中`)。
> · 横轴 · 渗透:`萌芽` → `早期采用` → `主流化`(判档看真实使用信号,不看 star / votes)。

> **🚫 去重铁律(这是本日报最该守的一条——以前同一条证据被复述 5-8 遍,可读性最差):每条证据全文只详展一次,放在它最该落的那条轴里。**
> · 头条结论 → 只在「一句话结论」点一句;
> · 「纵轴 / 横轴」→ 各自只讲自己那条轴的证据,写定档 + 卡点,不互相重抄;
> · 「原始证据附录」→ 只枚举 + 一句定性 + 指向哪条轴;已在两轴展开的只写指针(→纵轴矢量X),不重述;
> · 「横向速记」→ 只写关系,不复述单条证据;
> · 「本期变更」→ 只用 `T-编号` 引用。
> 同一句描述出现在第二个地方 = 失败,删掉或改成指针。

> **话术:讲人话。** 用日常说法把事说清楚,少用术语黑话和翻译腔;判断、档位还是要准。同一维度的多条目用 list,但每条说完整通顺的话,别写成"字段A - 字段B - 字段C"那样的横杠拼接。

## 一句话结论

2-3 句,把今天这张坐标系说清楚,覆盖三件事:

1. **纵轴(能力 / 技术)**:今天模型 / 产品的能力往哪动了——实验室加了什么能力、开源补了哪个痛点,有没有方向在收敛。
2. **横轴(渗透)**:今天 AI 产品有没有让更多人真用上(新载体 / 新用户段 / 新入口 / 真实使用量)。没有就直接说"今天渗透没新变化"。
3. 后续 24-72h 最该盯的 1-2 个对象或节点(谁可能跟进、哪条矢量可能进档)。

例:"纵轴上,Anthropic 把'长任务做得稳'从试水推到收敛中,开源那边又有 3 个项目接力做工具标准化;横轴上,能自己在后台干活的 agent 冒出 2 个普通人也能用的入口,但用的人还不多、仍算刚起步;接下来盯两件事——OpenAI 会不会跟,以及某项目正式版什么时候发。"

> AI 大盘里跟两轴无关的大事(纯刷榜、视频模型发布等),结论后一句话带过,不展开。

## 纵轴 · 能力与技术演进

> 这条轴回答:模型和产品的能力、技术今天往哪走。实验室侧(谁发了模型 / 加了能力)和开源侧(谁在解哪个工程痛点)的证据,**并到同一条矢量上**讲。同一矢量当天有 ≥2 个独立证据(理想一个实验室 + 一个开源)一起推,才算这条在收敛;单点只记、先不判收敛。

逐条矢量写,每条一段 list:

- **矢量名(一句话说清在解 agent 的什么能力问题)** —— 档位:实验 / 收敛中 / 事实标准
  - 实验室侧:[公司 / 来源] —— 加了什么能力、解锁了哪类 agent 行为
  - 开源侧:[项目 / 来源] —— 在解哪个痛点(编排 / 记忆与上下文 / 工具标准化 MCP / 可观测 / 沙箱 / eval / 成本可靠性 ...)
  - 还卡在哪(open_pain):差什么才能进下一档
  - 别高兴太早:[只是 demo / 还是单点 / 没人真用]

> 参考矢量库(别硬套,从当天证据自然归类):长任务做得稳 / 工具标准化(MCP) / 多 agent 协作 / 记忆与上下文 / computer use / 推理成本与小模型 / agent 测评 / 沙箱与运行时。

## 横轴 · 渗透率

> 这条轴回答:AI 产品今天有没有让更多人、更多场景真用上。**没有新变化就直接写"今日无新渗透信号",别硬凑一条。** 有的话,每条按五个追问写清:

| 形态 / 产品 | 载体 / 入口 | 用户段 | 自主度 | 真实使用信号(≠ star / votes) | 档位 |
|---|---|---|---|---|---|
| 后台自己干活的 agent | 无 UI / 直接交结果 | 开发者 → 专业用户 | 有人盯 → 自己跑 | 官网说跑了 N 个任务 / 有人付费 | 萌芽 |
| ... | | | | | |

> 档位:萌芽 / 早期采用 / 主流化。渗透说白了就是"原来用不了的人现在能用了",或者"老用户用得比以前多了"。
> 入口尤其要留意:嵌进大家已经天天用的工具(IDE / 浏览器 / 办公软件),比单独出个 app 铺得快得多。

表格下面可以加 1-3 条话,讲某个形态的关键设计、用户反馈或缺口——每条说完整,别只丢关键词。

## 原始证据附录

> 这一节是双引擎(GitHub Trending / Product Hunt / Show HN + 重点公司动作)当天捞到的原始 feed,只做"看到了什么 + 一句定性 + 归到哪条轴",**不重讲分析**。已经在两轴展开的对象,这里只写指针(→纵轴矢量X / →横轴形态Y);没进两轴的(纯大盘背景、还在观察的),给一句定性就行。某类别今天没有可核实动作就整段省略;中国厂商今天没有官方信源覆盖的,写"无可核实更新",别拿没确认的消息硬填。

| 对象 | 来源 | 热度 / 体量 | 一句定性 | 落点 |
|---|---|---|---|---|
| 某模型 | 官方 + 金十 | 开源权重 / MIT | 国产开源旗舰,能力一次给齐 | →纵轴矢量"国产开源底座" |
| 某项目 | GH Trending | ★ x,xxx (+xxx/d) | 把某 SaaS 接成 agent 工具 | →横轴"xxx 形态" |
| 某项目 | Show HN | score xxx / 评论 xx | TTS 模型,跟两轴弱相关 | 仅大盘背景 |
| ... | | | | |

> 对象定性可借统一标签:基础模型与权重 / Agent 框架与编排 / 开发者工具链 / 推理与部署 / 数据与 RAG / 应用产品 / 评测与基准 / 开源基础设施。

**资本与政策**(融资 / 并购 / 监管 / 人事,既不是能力也不是渗透,放这里;今天没有就省略整块):

- [主体] —— [类型] —— [关键金额 / 内容] [来源]
- ...

## 横向速记 & 降噪

**横向速记**(3-5 条,只写关系,不重抄证据):

- **供给 ↔ 需求对没对上**:纵轴的能力进展,有没有被横轴的某个新渗透接住?(例:computer use 成熟 ↔ 出现"帮你点网页"、普通人也能用的 agent);还是能力到位了却没人做成产品(或者反过来)?
- **开源 ↔ 公司**:今天公司没动的地方,开源补上了吗?还是公司喊了、开源还没跟上?
- **中美对照**:今天中外厂商各自扎堆在哪、各自缺哪块?(中国这边常常是应用先行)

**降噪**(哪些别过度解读,每条说清降级理由):

- 把一次 demo / 发布会演示当成"渗透落地"——横轴只认真实使用信号,不认 PR 稿
- 把 GH star / PH votes / HN score 当成渗透率或"赚到钱了"——这些只是关注度
- 把一次刷榜当成 agent 能力进步——得落到具体解锁了哪类 agent 行为
- 单源爆火、标题党、没法核实的融资 / 性能宣称
- 只有第三方转述、没经厂商官方确认的
- "Sora 杀手""GPT-5 平替"这类营销话术

## 待跟踪 & 本期变更

**未来 24-72h 待跟踪**(这三栏就是本期 watchboard 的投影,也是下一期的输入:待印证矢量 = `agent_eng_vectors`、待观察渗透 = `product_forms`、待发布 / 验证 = `watch_companies` + `watch_projects`):

| 待印证的能力矢量(纵轴) | 待观察的渗透(横轴) | 待发布动作 / 待验证项目 |
|---|---|---|
| 长任务可靠性能否从收敛中进事实标准 | 后台 agent 能否进普通人能用的入口 | OpenAI 是否跟进 computer use / xxx 项目正式版 |
| ... | ... | ... |

> 没线索的栏直接写"暂无明确预期",别拿空话凑。

---

### 🔄 本期变更(框架 × 跟踪合并)

> 放报告末尾:先写完两轴和证据,再回填本期 watchboard 的变化。对照 `Prior Watchboard`,没有上一期就写"冷启动,本期建立初始 watchboard"。本段是 watchboard 字段的渲染——框架移动取自 `frame_change`,跟踪项动作取自各项 `update`(顺延)/ `resolution`(结算);**先填字段、再照着渲染本段,别两头写得不一致**。

把本期相对上一期的变化**合并成一段**,框架与跟踪混编、按重要性排序,逐条 bullet,引用证据只用 `T-编号`、不重讲:

- **框架**(动了才写,没动就写"两轴矢量 / 渗透沿用,N 项顺延"):`agent_eng_vectors` 哪条矢量进 / 退档(实验 → 收敛中 → 事实标准)或增删、`product_forms` 哪条渗透档位变了或新增、`watch_companies` / `watch_projects` 增删——每条紧跟一句"为什么"(← `frame_change`)。
- **跟踪项结算**(上一期每个 open 项,状态机层面都要被"碰一次",但**呈现层必须消化臃肿**,详见 `watchboard.md`):
  - 正文只逐条展开"今天有动作"的项(有进展 / 新开 / 结算):`#T-xxx → ✅兑现(矢量进档 / 渗透上台阶 / 热度转正式版)/ ❌退潮 / ⌛过期`:一句话证据(← `update` / `resolution`)。
  - **纯顺延(说法没变、也没新证据)的别平铺**,按主题聚合成一句话列编号:`#T-101/104/105/110 无新进展,顺延`。十几条顺延平铺占满篇幅 = 失败。
- **新开**:`#T-xxx [新矢量 / 新渗透 / 新项目] —— 因为 [≥2 证据共振 / 某公司预告 / 出现真实使用信号]`。
