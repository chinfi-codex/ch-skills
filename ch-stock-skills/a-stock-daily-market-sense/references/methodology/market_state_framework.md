# 市场状态定位框架（模块 1.1 判断手册）

配套证据块：`module1_market_trend.json` 的 `market_state`（`scripts/market_state_card.py` 生成，位置分层 / 宽度 / 行业结构 / 融资 / 流动性）与 `extreme_state`（`scripts/extreme_state_card.py` 生成，底部出清分 / 顶部拥挤分）。两个脚本都只出确定性读数、不给结论；本手册是模型的判断框架。状态定性写进模板 1.1 的 `==市场状态定位==` 段，不超过 150 字，不含买卖建议。

## 判断目标

回答两个问题：

1. 当前回撤属于什么量级——权重指数与成长小盘分开定性（见"位置分层"）；
2. 调整是否接近尾声（见"状态确认三要素"）。

## 状态确认三要素（调整接近尾声的确认信号）

1. **出清分 ≥2**：`extreme_state.washout.score` 当日 ≥2 分，或 `washout.zone=true`（近 10 个交易日内出现过 ≥2 分）。逐条命中项在 `washout.legs` 里，引用时点名是哪几条（宽度极值 / 跌停占比 / 新低占比 / 融资 bp / 指数回撤 / 个股中位回撤）。当日 ≥3 分记"出清极值"。
2. **融资余额停止创新低**：`margin_trend.is_new_low_20d` 转 false，`days_since_20d_low` 开始累计，且 `chg_5d_pct` 走平或转正。融资是 T-1 口径，引用时必须带 `margin_trend.trade_date` 数据日。
3. **行业面扩散**：`sw_industries.count_positive_60d` ≥ **10** 个申万一级行业 60 日收益为正，**且伴随盈利上修**。盈利上修脚本不提供，须由模型从外部证据（业绩预告、券商一致预期等）判断；没有外部证据时写明"第三要素缺外部证据"。

三要素是"且"的关系，只满足一两条时写"修复进行中但未确认"。

> **不要等确认。** 2026-08 的 5 年回放（`evals/trend_state_review_2026-08.md`）显示：出清分这条腿在 7 个阶段底上的中位滞后是 **0 个交易日**、能吃到全部反弹；而"等宽度推力/上涨占比确认"的做法触发后 60 日只有 +0.03%~+0.61%，反而低于全样本基准的 +1.15%——收益全在出清那几天本身，等确认恰好把最肥的一段让掉。所以第一要素满足时就该照实写"已进入出清区"，不要因为后两条还没到就压着不说。

> **旧的第二要素已删除。** 原先写的是「宽度 `pct_above_ma60` 从低位恢复到 50% 以上」。实测它比阶段底晚 20 个交易日（中位），触发后未来 60 日 −0.83%、跌 ≥8% 的概率 42.6%（基准 32.4%），在 2021H2~2023 与 2024~2026 两个子样本里都跑输各自基准。它是个**反向**指标，不得再作为"调整接近尾声"的依据。`pct_above_ma60` 本身仍然是有用的宽度读数，照常列在状态表里，但只描述宽度水平，不承担确认职能。

## 顶部风险：只写累积度，不写方向

`extreme_state.top.score`（0~5）逐条列出命中项：宽度背离 / 赚钱效应衰减 / 杠杆加速 / 量能亢奋 / 宽度过热。≥2 分写"顶部风险累积"，≥3 分写"顶部高危"。

措辞纪律，三条都要守：

- **不得写成方向性结论。** 顶部侧的证据强度远低于底部：5 年回放里只有「杠杆加速」在两个子样本都跑输基准，而且只覆盖 7 个阶段顶中的 3 个；宽度背离虽然 7 个顶全都提前 20~28 天示警，但在熊市段没有增量信息。写"风险在累积"，不写"要跌了"。
- **必须带上"顶是过程不是点"这层意思。** 实测顶部高危触发后未来 20 日平均**还有 +2.03% 的上冲**，60 日均值才转为 −1.44%（基准 +1.15%）。所以不要把顶部分当成即刻的转折信号。
- **`percentile_source` 是 `fixed_fallback` 或 `rolling_quantile_capped` 时要说明**，那意味着 `dms_extreme_daily` 的历史还没攒够一个完整窗口，阈值取的是滚动分位与长期参考中更严的一侧。

## 两根轴冲突时并列写，不许调和

趋势轴（`trend_state_card` 的六档）是慢变量，描述所处阶段；极值轴（`extreme_state`）是快变量，抓两端拐点。二者经常不一致，这是设计使然而不是矛盾：退潮档里出现出清极值，恰恰是 A 股最常见的底部形态。

遇到冲突照实并列，例如"趋势仍在退潮（第 6 日），但出清分已到 4/6、进入出清区"。**不得**为了让两句话看起来一致而改写任何一边的读数，也不得只写其中一根轴。

## 位置分层（回撤量级参考）

权重指数（上证综指、沪深300）与成长小盘（中证1000、创业板指、科创50）分开看，用 `index_position` 的 `drawdown_from_high_250d_pct`。参考量级（非硬规则）：

- 回撤 < 10%：调整；
- 10–20%：深度调整；
- > 20%：接近"技术性熊市"表述。

权重与成长小盘回撤量级明显不同时，说明是结构性出清而非系统性下跌，措辞上区分"权重护盘、成长出清"这类分层，不要用一个数字概括全市场。

另外可用 `sw_industries.industries[].high_250d_date` 与 `benchmark_high_250d_date`（上证综指 250 日高点日期）比对：领涨行业高点日期明显早于宽基高点日期，说明主线已先于指数见顶，是结构退潮的直接证据。

## 纪律

- 证据不足时明确写"不足以判断"；脚本没有的数（如盈利预测）只能从外部证据引入并注明来源，不得编造。
- 融资读数是 T-1 口径，引用必须带数据日（`margin_trend.trade_date`），不能说成当日资金行为。
- `insufficient_history: true` 或字段为 null 时按缺失处理，不得用缺口外的数脑补。
- 某子块 `available: false` 时披露原因，其余子块照常使用。
- 本模块只定位市场状态，不输出买卖建议、仓位或目标价。**退潮 / 深度退潮 / 冰点是风险状态描述，不等于看空**——历史上冰点日之后 20 个交易日平均上涨 8.92%、81.8% 的时候在涨，写的时候不得暗示应当离场。
