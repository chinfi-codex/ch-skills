## 三、测试、评估与迭代

### 3.1 起手:写 2–3 个真实测试 prompt

要点:

- **像真实用户那样说话,不是教科书 prompt。** "帮我看看宁德时代最近怎么样"比"对宁德时代(300750.SZ)进行基本面分析"更接近线上分布。
- **包含一个边角 case。** 比如新股(财报数据不全)、ST 股、最近停牌、跨界并购等会触发降级路径的场景。
- **包含一个反例。** 一个**不该**触发该 skill 的 prompt,用来测 description 有没有过度泛化。

落盘到 `evals/evals.json`:

### 3.2 双层评估体系

单一打分方式会漏东西。建议**布尔断言 + Rubric 评分叠加**(不是替代关系):

- **布尔断言(expectations)**:判客观事实,二值,通过/没通过。例如"输出文件必须是 .xlsx"、"必须包含 ROE 字段"、"调用了 `fetch_daily_kline.py`"。**保证不漏硬伤。**
- **Rubric 评分(rubric_scores)**:质量光谱,1–5 分带锚点。例如"输出内容质量"、"主动思考性"。**捕捉细腻度。**

两层落到不同字段,各自聚合,不要混。

### 3.3 五大评估维度

不是每个 skill 都要打全五维。先想清楚 first-class metric 是哪两三个,其他作辅助。**维度太多反而稀释信号。**

#### 维度 1:指令遵循度

实际有两层,分开打分:

- **用户指令遵循**:prompt 里明写的约束(字数、格式、必含/必排除内容)
- **Skill 指令遵循**:SKILL.md 规定的工作模式(必须先调某脚本、必须按某结构输出、必须先读某 reference)

第二层尤其关键 —— 如果 agent 压根没按 skill 走,你测的根本不是 skill 的效果,而是底模的裸表现。**这一层必须看 transcript,不能只看 output。**

锚点:

- 1 分:忽略 ≥1 个关键约束(如要 docx 给了 markdown)
- 3 分:主体遵循,遗漏 1–2 个次要约束
- 5 分:所有显式 + 隐式约束完全遵循

#### 维度 2:数据完整度

最适合**程序化打分**,因为完整度本质是覆盖率。

- 每条 eval 准备一份 "expected coverage" 清单(应有的字段/记录/章节)
- 脚本逐项核对
- 用覆盖率映射到 1–5

锚点:

- 1 分:缺失 >50% 应有数据
- 3 分:主要项齐全,次要项漏 1–2 处
- 5 分:100% 覆盖

**坑点**:警惕"看起来全但其实编了"。让 grader 在打分时同时输出 `evidence` 字段,引用原始数据出处,否则容易把幻觉数据也当成已覆盖。

#### 维度 3:输出内容质量

最难、最主观。建议拆成 4 个子维度独立打分,再聚合:

- **准确性**:事实/数据/计算是否正确
- **逻辑性**:论证链条是否站得住
- **专业性**:术语、语域、行业规范是否到位
- **可读性**:组织、节奏、可扫读性

打分必须用**强模型 grader + 详细 rubric**,并且**做校准**——同一份产出让 grader 跑 3 次,看打分方差。方差大说明 rubric 不够清楚(或这个维度对该 skill 来说就是没法稳定评的),要么换维度,要么靠人。

**这一项人工 spot-check 不能省。** LLM grader 在内容质量上最常犯的错就是被流畅自信的废话蒙过去。

#### 维度 4:主动思考性

诀窍是:**别在 grader 这一侧努力,去 eval prompt 这一侧努力。**

设计 eval 时**故意埋钩子**,然后看产出有没有钩到:

- 数据分析任务:数据集塞 2 行明显异常值(负销售额、未来日期),看产出是否提示
- 编码任务:需求里留一个隐含歧义("删掉旧数据"没说时间界定),看产出是否主动澄清
- 投研任务:给一份财报,财务数据正常但经营性现金流和净利润背离,看产出是否注意到

这样"主动思考"这个虚的维度就被翻译成"是否提到了那 N 个钩子"这种近似布尔的判断,可靠度高一个量级。

锚点:

- 1 分:完全字面执行,所有钩子视而不见
- 3 分:捕到 1/3 钩子,但分析浅
- 5 分:捕到大多数钩子且分析有深度

#### 维度 5:主动创新发现性

最难自动化,基本脱不开人工。两个思路:

- **思路 A:对比法。** grader 同时看 with_skill 和 without_skill 的产出,回答"with_skill 里有哪些洞察是 without 里没有的?",然后由领域专家(你)给"增量洞察"逐条打有用度。
- **思路 B:盲评法。** 多版 skill(v1/v2/v3)的产出脱标签,grader 选最有洞察的一版,多轮统计胜率。

锚点:

- 1 分:纯执行型,零增量发现
- 3 分:偶有有趣点,但表述模糊或不深入
- 5 分:稳定产出用户事后会引用的非显然洞察

**核心坑点:这一项极易被 gaming。** 如果 SKILL.md 里写"必须包含 3 个反直觉的洞察",agent 一定会编出 3 个看起来反直觉的废话。所以这一维度的得分**只在与基线对比中有意义**,绝对值没什么参考价值。

#### 辅助维度

按需启用,不必每次都打:

- **格式合规性**:文件类型、表头、字段名、文档结构,偏布尔
- **稳定性**:跨次运行的方差,本质是 benchmark 里的 stddev
- **效率成本**:耗时和 token,benchmark 里直接有
- **可追溯性**:研究/投研类 skill,每个关键结论是否标注出处

### 3.4 grading.json 与 benchmark.json 结构

**`grading.json`**:每次单独运行的判分结果,每个 eval 的每次运行各一份:


### 3.5 实操经验

跑测试过程中容易踩的坑,提前知道能省半天:

- **别每个 skill 都打全五维。** 数据提取类 skill 主看维度 1+2,投研分析类主看 3+4+5。先确定 first-class metric。
- **权重起步等权 1.0。** 跑 2–3 轮以后,看哪个维度的得分变化跟你"直觉上的好坏"相关性最高,再加权。一开始拍脑袋设权重大概率会偏。
- **rubric 必须锚点化。** "3 分代表中等"是无效的,必须写"3 分意味着主要数据齐全但漏了次要项"。否则不同次跑分会漂。
- **LLM grader 要校准。** 同一份产出让 grader 跑 3 次,方差 >1.0 的维度基本不可信,要么改 rubric 要么这维度只能靠人。
- **人工抽检永远保留。** 特别是输出质量、主动思考、创新发现三项。自动分高 ≠ 真的好;自动分低也可能是 grader 不识货。每轮抽 2–3 个 case 自己看一眼,比加一堆维度有用得多。

### 3.6 迭代闭环

每一轮迭代固定流程:

1. **冻结测试集**(同一轮内不改 prompt,否则没法对比)
2. **跑两组**:with_skill + without_skill(或 new_version + old_version)
3. **生成 grading.json + benchmark.json**
4. **人工抽检** 2–3 个 case
5. **定位 gap**,问自己:
   - 是 description 没触发? → 改 §2.3
   - 是方法论不到位? → 改 §2.4 或拆 references
   - 是工作流跳步? → 改 §2.5
   - 是输出风格漂? → 改 §2.7
   - 是脚本数据漏? → 改脚本而不是 SKILL.md
6. **回归检查**:改完之后,**老的测试 case 是否还过**?避免"按下葫芦起了瓢"
7. 进入下一轮