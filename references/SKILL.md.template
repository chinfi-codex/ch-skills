---
name: skill-name
description: 触发的唯一字段。把"何时使用 + 做什么 + 不做什么"写在这里。用场景化语言，覆盖同一意图的多种表达，正例反例同写。
---

# Skill Name

## 目标

1. **做什么**：一句话概括产出形态。
2. **不做什么**：划清边界，避免过度触发。
3. **给谁用**：面向的使用者与典型场景。

## 适用场景与边界

在 description 已写过的基础上展开。明确什么情况下触发、什么情况下不触发、典型输入和例外。

## 领域方法论

把本领域内反复使用的思考框架显式写下来。如果超过 ~150 行，拆到 `references/philosophy.md`，这里只留纲要 + 索引。

## 工作流程

按"可独立验证的产物"切分步骤。每一步说明：做什么、用什么（脚本/reference/上下文）、产出什么。

1. 步骤一
2. 步骤二
3. 步骤三

## 数据获取（脚本抓手）

| 脚本 | 职责 | 调用方式 | 必需参数 | 返回格式 |
|---|---|---|---|---|
| `scripts/foo.py` | 一句话说明 | `python scripts/foo.py --arg value` | `arg` | JSON |

依赖的环境变量：`TUSHARE_TOKEN`、`OPENAI_API_KEY` 等。

## 输出规范

| 维度 | 怎么做 | 为什么 |
|---|---|---|
| 结构 | 标题层级、必选/可选段落 | 便于对比多份产出 |
| 长度 | 给字数或段落数上限 | 防止越写越长 |
| 风格 | 中立分析性 / 报告性 / 口语化 | 保持一致 |
| 数据呈现 | 小数位、单位、表格 vs bullet | 统一跨数据源格式 |
| 附加要求 | 免责声明、时间戳、来源 | 可追溯 |

## 示例

### 输入

用户的原始请求示例。

### 输出

符合输出规范的示例产出。
